# =============================================================================
# LOCAL DEVELOPMENT CONFIGURATION
# =============================================================================

# Disable GCP SQL for local development
spring.cloud.gcp.sql.enabled=false

# Local development logging
logging.level.com.investment.metal=DEBUG
logging.level.org.springframework.web=DEBUG

# Security overrides for local development
metalinvestment.cors.allowed-origins=http://localhost:3000
metalinvestment.csrf.cookie-secure=false
metalinvestment.csrf.cookie-samesite=Strict
